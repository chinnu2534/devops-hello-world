version: '3.8'

services:
  web-app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    # Mounts the current directory to /app so you can edit code without rebuilding
    volumes:
      - .:/app